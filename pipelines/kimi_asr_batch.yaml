# 工序②-c：独立执行全量 Kimi-Audio。
# audio-data pipeline run pipelines/kimi_asr_batch.yaml --source-name mt3000
# → cleaned_mt3000 → kimi_asr_mt3000.parquet
# 每个 shard 是独立进程/模型实例；显存不足时把 instances_per_gpu 降为 1。

name: kimi_asr_batch

input:
  manifest: datasets/manifests/cleaned_source_A.parquet

output:
  manifest: datasets/manifests/kimi_asr_source_A.parquet

execution:
  executor: sequential
  workers: 1
  fail_fast: false
  checkpoint_every: 500

sharding:
  shards: 4
  strategy: duration-balanced
  parallel_shards: 4
  gpus: [0, 1, 2, 3]
  instances_per_gpu: 1
  workers: 1
  executor: sequential

pipeline:
  - name: kimi_asr
    operator: asr.kimi_batch
    params:
      input_audio_key: resampled_16k
      config_path: configs/asr/kimi_audio.yaml
