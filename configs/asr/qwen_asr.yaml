model: qwen3-asr
model_version: "Qwen3-ASR-1.7B"
model_path: /data2/data-cp/zcl/models/Qwen3-ASR-1___7B
api_url: null
# 设置后改用 vLLM OpenAI 兼容接口；也可通过 QWEN_ASR_API_BASE 覆盖。
# 支持 http://host:port、http://host:port/v1 或完整 transcriptions URL。
api_base: null
api_key: dummy
concurrency: 4
timeout: 120
language: Chinese
device_map: cuda:0
dtype: bfloat16
batch_size: 32
max_new_tokens: 256
return_time_stamps: false
temperature: 0
response_format: json
context: "vocabulary:没有，暂时不用，不需要谢谢，不可以啊，不用，我不需要，不要，不用，不需要"
