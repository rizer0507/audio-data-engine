# 数据挑选规则（现行）

> 本文描述**当前生产默认**的分拣规则，与 `configs/selection/zh_asr_v1.yaml`（`selection_zh_asr_v1_6` / `rule_version=selection_v1.1`）及 [工序总览](../01-项目架构/工序总览.md) **工序一**（分拣 / 金标阶段，原引擎③）一致。
>
> 实现入口：`quality.classify` → `audio_engine.core.selection_engine`（`engine: consensus_v1`）。
>
> **并行可选 v2**：`selection_v2.0` / `consensus_v2` 已落地，见 [数据挑选规则_v2.0](./数据挑选规则_v2.0.md)；配置 `configs/selection/zh_asr_v2.yaml`，流水线 `pipelines/classify_dataset_v2.yaml`。v2 将模型共识标为伪金标，正式评测须人审/外源金标。需求归档：[011](../04-改进需求/已完成/011-数据挑选规则v2落地.md)。
>
> **v3 工程已齐、生产未切**：`selection_v3.0` / `classify_dataset_v3` / `build_dataset_v3` / `business_metrics_v1` 已落地（[012](../04-改进需求/进行中/012-细分需求索引.md)）。**人工校准与业务门禁通过前，生产默认仍为本文件所述 v1.1，不得改默认配置。**
>
> 历史讨论稿见 [010-挑选规则重构](../04-改进需求/已完成/010-挑选规则重构.md)；旧完善草案 [003](../04-改进需求/进行中/003-挑选规则完善草案.md) 已过时。

## 在整体链路中的位置

分拣是「清洗 → 多模型识别」之后、「评测集 / 训练集」之前的一步。按是否自带金标分两条（全图见 [工序总览 · 两条数据入评测主路径](../01-项目架构/工序总览.md)）：

```text
路径 A · 带外源金标
  清洗 → 多 ASR → classify_external_gold（注入外源金标 + 分桶标签）→ 评测集

路径 B · 不带金标
  清洗 → 多 ASR → classify_dataset（共识匹配候选金标 + 分桶标签）→ 评测集
```

本文主体描述**路径 B**（`zh_asr_v1` / `consensus_v1`）。路径 A 见文末「外源金标」与 [006](../04-改进需求/已完成/006-工序一清洗引擎拆分需求.md)。

使用 v2 时路径 B 产出 `pseudo_gold_*`，**不得**默认 `eval register` 为正式评测真值（须 `--allow-pseudo-gold` 调试，或人审升 `human_gold` / 外源路径）。

## 配置入口

- 策略文件：`configs/selection/zh_asr_v1.yaml`（默认）
- v2 策略：`configs/selection/zh_asr_v2.yaml` + `semantic_lexicon_zh_v2.yaml`
- 语音信箱正则：`configs/selection/voicemail_patterns_v1.yaml`
- 分拣算子：`classify_dataset` / `quality.classify`

## 三个字段拆开

| 字段 | 含义 | 取值示例 |
| --- | --- | --- |
| `type` | 这条数据是什么问题 | `auto_gold` / `consensus_gold` / `hardcase` / … |
| `decision` | 最终怎么处理 | `auto_accept` / `auto_empty` / `model_review` / `manual_review` |
| `label` / `gold_text` | 最终金标文本 | 非空字符串或空 |

另写入审计字段：`rule_version`、`selected_model`、`support_models`、`support_count`、`support_family_count`、`consensus_score`、`min_similarity`、`semantic_qwen`、`semantic_sensevoice`、`review_reason` 等。

v2 额外字段：`label_source` / `label_tier` / `is_human_verified` / `dataset_role` / `subtype` 等，见 [v2 规范](./数据挑选规则_v2.0.md)。

## 核心概念

### transcript_similarity（不是「字准率」）

挑选阶段没有人工金标，模型两两比较得到的是**转写一致度**：

```text
similarity(A, B) = 1 - edit_distance(A, B) / max(len(A), len(B))
```

正式阈值：

| 常量 | 默认 | 含义 |
| --- | --- | --- |
| `STRICT_THRESHOLD` | 0.95 | 高度一致 → `auto_gold` / voicemail 严格自动金标 |
| `CONSENSUS_THRESHOLD` | 0.90 | 多数派 cluster 内部一致门槛 |

### 模型族（model_family）

同族多跑不算独立票：`qwen1+qwen2` 只算 Qwen family。默认族映射见配置 `model_families`。

### 金标选取：medoid（禁止随机）

在候选转写中选与其余文本平均 similarity 最高者；并列时按 `model` 名字典序稳定打破。

## 判定顺序（严格）

```text
文本标准化
  → 损坏/时长≤0 或全部为空？ → noise (auto_empty)
  → ≥2 模型族命中 voicemail？ → voicemail
  → 主/次族 positive↔negative 冲突？ → semantic_inversion
  → 主族全空且其他非空？ → qwen_missing
  → 空占比≥75% 且非空仅单族？ → hallucination
  → 全部非空且 min_pairwise≥0.95 且语义一致？ → auto_gold
  → ≥75% dominant cluster（内部≥0.90、跨≥2 族、语义一致）？ → consensus_gold
  → hardcase
```

## 分拣桶

| 优先级 | type | 核心含义 | 典型 decision |
| ---: | --- | --- | --- |
| 1 | `noise` | 损坏/无效，或全部模型为空 | `auto_empty`，label 空 |
| 2 | `voicemail` | ≥2 模型族命中语音信箱正则 | 一致则 `auto_accept`，否则 `manual_review` |
| 3 | `semantic_inversion` | 模型族间肯定/否定冲突 | 默认 `model_review`；≥2 独立族多数可 `auto_accept` |
| 4 | `hallucination` | 空占多数且非空仅来自单族 | 通常 `model_review`；≥3 族且其余皆空可 `auto_empty` |
| 5 | `qwen_missing` | 主族（默认 Qwen）全空、其他非空 | `model_review` |
| 6 | `auto_gold` | 全部非空且高度一致 | `auto_accept` + medoid |
| 7 | `consensus_gold` | 明显多数派跨族一致 | `auto_accept` + cluster medoid |
| 8 | `hardcase` | 无法形成可靠共识 | `model_review` |

兼容说明：旧桶名 `review_queue` 仅保留在 expr 遗留模式与 CLI 人审默认列表中；现行引擎输出 `qwen_missing`。

## 工序衔接

- `audio-data review export-gold`：默认导出 `auto_gold` + `consensus_gold` + `pseudo_gold_high`（且 `decision=auto_accept`）
- `audio-data review export`：默认导出待审桶（含 v2 风险桶）
- 人审可搁置；`--allow-unresolved-review` 允许仅用已有金标冻结 Release
- **正式评测**：`eval register` 默认 `--formal`，拒绝伪金标；外源 / `human_accepted` 可进。调试用 `--allow-pseudo-gold` 或 `--legacy`
- **外源金标**（`external_gold_v1`）：`classify_external_gold` 注入 xlsx 后分拣；`gold_text` 以外源为准；四桶 `voicemail` / `noise` / `auto_gold` / `hardcase`。见 [006](../04-改进需求/已完成/006-工序一清洗引擎拆分需求.md)

## 遗留模式

若配置显式提供 `rules:` 且未设 `engine: consensus_v1` / `consensus_v2`，仍走旧版 pandas `expr` 有序规则（单测与兼容用）。**现行生产配置使用 consensus_v1 引擎。**
