> 复制本文件 → 另存为 `docs/需求/<随便起个名字>_需求.md`，用自然语言把想法写清楚，交给 AI 即可。  
> **不用填表格、不用写 YAML、不用懂算子名。** 技术细节由 AI 按 `docs/流水线构建执行手册_AI.md` 补全。

---

## 我想做什么

当使用qwen流水线/sensevoice流水线或者是更新后的模型识别的流水线之后，我想把他们识别的结果汇总在一个表格中。识别结果的流水线请单参考docs文件夹中的对应流水线。汇总的数据源需要有一个参数来指定，另外我需要计算编辑距离（总+错字+少字+多字），最终我需要把所有的结果汇总在一个xlsx表格中，我会提供一个参考的脚本，但是这个参考脚本中有很多字段是固定字段，我需要进行灵活配置化。


## 数据从哪来、结果要什么

- 输入大概是什么？（例如：已经清洗好的音频 / 某个文件夹 / 上一条流水线的结果）
是不同版本的模型跑出来的结果
- 跑完我希望得到什么？（例如：带 Qwen 识别结果的表、筛掉坏音频后的清单……）
一个总的xlsx有单条数据不同的识别结果并且能够计算编辑距离
- 大概多少条数据？（有数就写，没有就写「不清楚」）
十万级别



## 业务上有哪些规矩

qwen模型识别都需要带上context，context指定为"vocabulary:需要/不需要，用/不用，要/不要，我需要/我不需要"


## 我怎么才算满意

能够自由配置我需要汇总的模型识别结果，并将结果汇总在一个表格中，清晰看到单条数据来源以及编辑距离

## 其他我想说的

重点参考我给你的脚本



---

## 交给 AI 时可以说

```text
请严格按 docs/流水线构建执行手册_AI.md 执行。
需求文档：docs/不同模型识别结果提取并流水线.md
用自然语言需求即可；缺关键信息先问我，其余工程细节你定。
做完更新 单条流水线执行命令.txt，保证一条 pipeline run 能跑。
```
