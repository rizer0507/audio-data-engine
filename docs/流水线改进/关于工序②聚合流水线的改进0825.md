当前的multi_asr_aggregate.yaml流水线还是设计的耦合度太高。
当前的问题是：
- sensevoice必须满足qwen才能出结果。但是我后面还是会加入其他模型的，那sensevoice的这条流水线就不可以复用。
- 计算字准率的过程也耦合到sensevoice中了，后面我需要加入其他模型，那就没有办法在当前工序②中计算了
- 计算字准率并没有形成单独的流水线过程

改进：
- 首选去掉字准率计算和sensevoice的耦合，完全剥离并删掉这个逻辑。qwen识别和sensevoice都单独用一个pipeline。
- multi_asr_aggregate.yaml将根据传入的参数来聚合不同的pipeline结果。将qwen_asr识别的结果和sensevoice识别结果还有后续加入的模型的识别结果（都在datasets/manifest/中，比如qwen_asrxxxxx.parquet，sensevoicexxxxxx.parquet,kimi_asrxxxxx.parquet等）
- 拿到multi_asr_aggregate.yaml处理后的parquet之后调用字准率计算.yaml来计算字准率。pipeline的设计文档待会我会给出。关于字准率的计算我给出了一份详细的文档
这里需要注意的是，在计算字准率的时候要确保所有的数据的id是对齐的，并且要表明来源。
