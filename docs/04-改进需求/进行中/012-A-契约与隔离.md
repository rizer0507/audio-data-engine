# 012-A · 契约与隔离

> 细分来源：[012-四家族双跑分拣与人工金标数据集闭环重构](./012-四家族双跑分拣与人工金标数据集闭环重构.md)（母文档，**不得删改**）
> 索引：[012-细分需求索引](./012-细分需求索引.md)
> 状态：**工程实现完成（2026-09-10）**；人工数据验收不适用本阶段（无听审/金标）。本文定义目标行为；下列「实现清单」记录已落地能力。
> 版本：`selection_v3.0` / `annotation_v3.0` / `dataset_policy_v3.0` / `business_metrics_v1.0`。
> 输入：Kimi-ASR、GLM-ASR、SenseVoice、Qwen-ASR 四个家族，每家族两份全量识别结果；微调对象为 Qwen-ASR。
> 实施范围（本阶段）：八路身份契约、空/失败区分、分组、评测预留与 immutable artifact；对应母文档阶段 **A：契约与隔离**。
> 本需求统一承接进行中 007、008、009 的相关目标，取代其与母文档冲突的设计；v1/v2 保留用于复现。不得直接覆盖旧分拣产物。
> 下游依赖本阶段产出： [012-B](./012-B-质量与分拣.md)、[012-C](./012-C-人工审核与伪标签门禁.md)、[012-D](./012-D-数据集构建与Release.md)、[012-E](./012-E-业务评测与生产切换.md)。

## 实现清单 / 测试 / 阻塞（工程）

### 已交付

| 项 | 落点 |
| --- | --- |
| 八路家族配置与 run 身份类型 | `core/selection_v3/config.py` / `types.py` |
| 空/失败/缺失区分 + 守恒报告 | `core/selection_v3/input_contract.py` |
| 泄漏组连通分量 + 缺元数据治理隔离 | `core/dataset_v3/grouping.py` |
| 不可变 reservation（eval_random / core_reserve / calibration / dev / train_pool） | `core/dataset_v3/reservation.py` |
| 生产入口 | `pipelines/prepare_dataset_v3.yaml` + `quality.prepare_dataset_v3` |
| 数据集契约配置 | `configs/datasets/zh_asr_v3.yaml` |
| `aggregate_manifests` 原音频哈希对齐 + 八路完整性报告 | `operators/quality/aggregate_manifests.py` v2.2.0 |
| `eval_ready` 扩展 leakage_group / source_audio / 内容哈希 | `core/eval_ready.py` |
| CLI `--config` 注入 | `audio-data pipeline run ... --config configs/datasets/zh_asr_v3.yaml` |

### 测试结果

- `tests/test_selection_v3_contract.py`：14 passed（契约、守恒、分组、预留确定性、算子、聚合报告、eval 泄漏）
- 回归：`test_text_metrics` / `test_source_naming` / `test_selection_v2` 相关用例通过
- 冒烟：`PipelineRunner` 跑通 `prepare_dataset_v3`

### 剩余阻塞 / 边界（不阻塞本阶段关闭）

1. **ASR 侧尚未强制写入** `run_id` digests / `created_at`；契约可接收，复制双跑检测依赖 digests 齐全时才 fail-fast。
2. **近重复指纹算法本身未实现**：仅消费已确认的 `near_duplicate_group_id` 与不确定标记字段。
3. **PCM 哈希未自动计算**：仅消费已有 `pcm_sha256` / `normalized_pcm_sha256`。
4. 分类 / DNSMOS / 人审 / Release → **012-B / 012-C / 012-D 工程已完成**；业务评测与生产切换 → 012-E。

### 人工数据验收

本阶段无听审与金标产出；**人工验收：不适用 / 未开始**。

## 0. 本阶段交付目标（母文档 §16.1）

**A：契约与隔离**——八路身份、空/失败区分、分组、评测预留与 immutable artifact。

每阶段分别提供实现清单、测试结果和剩余阻塞；工程完成与人工数据验收完成分开记录。

## 1. 目标与硬约束（母文档 §1，全局适用）

交付可复现链路：

```text
数据源快照 + 音频分组/查重 + 八路 ASR
  → DNSMOS 独立评分
  → 四家族状态与业务风险分析
  → 候选分类 + Qwen 纠错价值 + 人审队列
  → 人工听审 / 双审 / 仲裁 / 伪标签抽检
  → train / dev / eval_core / eval_random 独立冻结
  → Qwen Base / LoRA / Full SFT 在相同评测集上的 CER + 业务风险指标
```

必须满足：

1. 同家族两次推理用于发现不稳定性，最多计一票；四家族也可能共同出错，共识不等于真实标签。
2. Qwen 不担任金标基准，不享有投票加权；Kimi、GLM、SenseVoice 是三个辅助家族，不预设某一个绝对正确。
3. 任何一路出现业务语义冲突都须保留，禁止用族内 medoid 或多数票掩盖。
4. 正式评测仅用经验证的人工/外源金标；伪标签只允许进入通过抽检门禁的训练子集。
5. 输入已是上游切分段，不新增、不调用、不依赖 VAD；不生产 `speech_ratio` / `vad_edge_risk`。
6. DNSMOS 只衡量音频质量，不能判定无人声、串音、说话人归属或语义正确性。
7. 原始音频与八路原文只读；禁止自动拼接多个转写创造标签，禁止将“不”“嗯”等关键内容清空。
8. 所有阈值、词表、配额、审核规则版本化；校准未通过时允许分拣和送审，禁止自动发布伪标签训练集。

## 2. 本阶段必须复用与修复的现有入口（母文档 §2 相关条目）

- `quality.aggregate_manifests`：复用对齐与哈希校验，扩展八路输入完整性报告。
- `core/eval_ready.py` 当前 ID/重复组检查需扩展为同源片段、通话组、音频内容检查。
- （结构约束）`core/selection_v2/`：借鉴模块拆分，新建 `selection_v3/`；不得把新规则继续堆到 `selection_engine.py`。（本阶段先落地输入契约与类型；分类逻辑见 012-B）

母文档 §2 其余条目由后续细分承接，完整列表如下（不得遗漏，归属标注）：

- `core/selection_v2/`：借鉴模块拆分，新建 `selection_v3/`；不得把新规则继续堆到 `selection_engine.py`。→ **A 结构约束 / B 实现**
- `quality.aggregate_manifests`：复用对齐与哈希校验，扩展八路输入完整性报告。→ **A**
- `review export/import`：扩展字段、双审状态和空金标；现有 `accepted` 必须非空的限制不能沿用到 v3。→ **C**
- `release build`：v3 不使用“所有 accepted 混在一起随机拆 train/dev/test”的逻辑；先锁定用途，再按不同集合的标签门禁冻结。→ **D**
- `core/selection_v2/dataset_builder.py` 当前仅赋候选用途，不能视作已完成配额抽样。→ **D**
- `metrics/runner.py` 当前仅接受 CER，扩展同一指标入口，禁止独立脚本再实现一套业务指标。→ **E**
- `core/eval_ready.py` 当前 ID/重复组检查需扩展为同源片段、通话组、音频内容检查。→ **A / E**
- `noise_risk` 当前主要提取和透传，必须新增实际消费判定，不能只增加字段。→ **B**

## 3. 输入契约与八路对齐（母文档 §3 全文）

### 3.1 显式家族配置

```yaml
engine: consensus_v3
rule_version: selection_v3.0
target_family: qwen
model_families:
  kimi: [kimi_1, kimi_2]
  glm: [glm_1, glm_2]
  sensevoice: [sensevoice_1, sensevoice_2]
  qwen: [qwen_1, qwen_2]
teacher_families: [kimi, glm, sensevoice]
expected_runs_per_family: 2
```

别名可配置，不从文件名前缀猜家族。Kimi 本地/vLLM 两种运行方式仍属于同一 Kimi 家族。

每份结果登记：`run_id`、`family`、`model/checkpoint_digest`、`decode_config_digest`、`prompt_digest`、`input_audio_digest`、`input_audio_key`、`created_at`。同一产物复制两次不得算“双跑”；两次真实独立执行即使参数和文本完全相同也可接收。

每条音频必须有稳定 ID、原始路径、`sha256`、`duration`、`source_snapshot_id`、`source_audio_id`、分组字段。兼容旧 ID，但跨源必须使用带来源命名空间的唯一键。

### 3.2 状态不能与文本混淆

每一路显式区分：`success_text`、`success_empty`、`failed`、`missing`。

- 以音频快照为底表对齐八路，校验 ID、原音频哈希及血缘；不同推理音频变体通过原音频血缘关联，不比较 pad 文件与原文件哈希相等。
- 重复 ID、家族配置重复、相同 ID 对应不同原音频：整批 fail-fast。
- 推理侧额外 ID 输出报告；正式八路快照禁止混入其他数据源。
- 单条 missing/failed 保留为 `inference_incomplete`，生成补跑清单，不当作空转写，不进自动伪标签。
- 守恒报告：输入数 = 物理无效数 + 推理不完整数 + 可分类数；质量评分失败另列交叉统计。
- 质量侧车和所有聚合字段必须按 ID + 原音频哈希连接，禁止按行位置连接。

## 4. 数据隔离：先保留评测，再采训练（母文档 §9 全文）

### 4.1 泄漏组

按以下关系建无向图并取连通分量为 `leakage_group_id`：

- 同 `call_id/conversation_id`；有可靠客户/说话人标识时按配置并入同组。
- 同 `source_audio_id` 的切片、增强版本。
- 相同文件哈希或规范化解码 PCM 哈希。
- 已确认近重复指纹关系；保存算法、阈值与匹配证据。

分组字段必须从来源索引解析，不能盲猜文件名前缀。缺来源/通话映射的样本可分拣和人审，但进入治理隔离池；首版不得发布到声称无通话泄漏的正式训练/评测组合。元数据补齐后再解锁。

相同内容去重在训练中只保留代表，不删除历史记录；随机评测按原始目标分布采样，允许相同内容保留出现频次，但推断时按泄漏组处理相关性。近重复不确定匹配先隔离确认，不以相同文本判音频重复。

### 4.2 固定分配顺序

1. 冻结原始输入快照、原始 ID 列表、分组映射与目标分布定义。
2. 在查看模型错误和调整分拣规则前，按原始样本 ID 等概率随机选 `eval_random`，锁定其全部泄漏组；种子和完整候补序列固化。
3. 从剩余组预留 `eval_core_reserve`（默认按组哈希 15%）；其余为开发池。专项集只从该 reserve 选取，避免边挑训练边反向挪评测。
4. 开发池中独立预留 calibration 和 dev 组，剩余才是 train pool。默认 dev 占开发池组数 10%，calibration 按目标条数逐组选取。
5. 规则与质量阈值只用 calibration，模型/训练参数只用 dev。专项集构建规则冻结后才在 eval_core_reserve 分拣、抽样、听审。
6. 所有未入选的 eval 保留组仍禁止进入当轮 train/dev/calibration；禁止因难标、空标、Qwen 已识别正确而回流。

如历史模型结果已被查看，记录评测设计形成时间与已使用信息；不宣称历史数据是从未观察的独立测试。新微调结果不得参与修改已冻结评测集。

## 5. 本阶段开发交付与执行入口（母文档 §14 相关）

建议模块（本阶段相关）：

```text
core/selection_v3/
  config.py / types.py / input_contract.py
core/dataset_v3/
  grouping.py / reservation.py
configs/datasets/zh_asr_v3.yaml
```

生产入口继续使用 PipelineRunner；本阶段新建 YAML（名称为待实现契约）：

1. `prepare_dataset_v3.yaml`：输入校验、分组、预留 random/core/calibration/dev，输出不可变 reservation。

复用并扩展 `eval register/check`（与 E 协同），不另建旁路脚本体系。

CLI 示例在实现后必须成为可运行命令：

```bash
audio-data pipeline run pipelines/prepare_dataset_v3.yaml --config configs/datasets/zh_asr_v3.yaml
```

`--config` 若现有 pipeline CLI 尚不支持，新增配置注入且保留旧参数语义；八路 artifact、reservation、质量产物、审核 revision、train_size、release_id 必须从已解析配置/Catalog 显式获取，不靠寻找“最新文件”。

不重构 ASR 推理、不训练 DNSMOS、不建设分布式平台、不自动启动模型微调；本需求交付可供外部训练框架消费的正式数据。

母文档 §14 完整模块与入口清单（归属标注，不得遗漏）：

```text
core/selection_v3/
  config.py / types.py / input_contract.py          # A
  family_evidence.py / semantic_risk.py / consensus.py  # B
  classifier.py / review_router.py                  # B
core/dataset_v3/
  grouping.py / reservation.py                      # A
  sampling.py / audit.py / release.py               # D（audit 与 C 协同）
operators/quality/dnsmos.py                         # B
configs/selection/zh_asr_v3.yaml                    # B
configs/selection/semantic_lexicon_zh_v3.yaml        # B
configs/quality/dnsmos_p835.yaml                    # B
configs/datasets/zh_asr_v3.yaml                     # A / D
configs/annotation/zh_asr_v3.yaml                   # C
configs/metrics/business_risk_v1.yaml               # E
```

生产入口 YAML：

1. `prepare_dataset_v3.yaml` → **A**
2. `audio_quality_sidecar.yaml` → **B**
3. `classify_dataset_v3.yaml` → **B**
4. `build_dataset_v3.yaml` → **D**

复用并扩展 `review export/import` → **C**；`eval register/check` → **A / E**。

完整 CLI 链路（实现后必须可运行；本阶段交付第 1 条）：

```bash
audio-data pipeline run pipelines/prepare_dataset_v3.yaml --config configs/datasets/zh_asr_v3.yaml
audio-data pipeline run pipelines/audio_quality_sidecar.yaml --source-name "$BATCH"
audio-data pipeline run pipelines/classify_dataset_v3.yaml --source-name "$BATCH"
# review export → 独立听审/复核 → review import；具体参数在实现时同步 CLI --help
audio-data pipeline run pipelines/build_dataset_v3.yaml --config configs/datasets/zh_asr_v3.yaml
# 固定 eval release → 各模型独立跑批 → eval_aggregate → eval_metric_pipeline
```

顶层 Task DAG 在等待人工时保存 waiting_review 状态；可输出队列与缺口，不能模拟人审自动跨过。→ **C / D 协同**

## 6. 本阶段验收用例（母文档 §15.2 隔离与可复现相关全文收录）

以下为母文档 §15.2「标注与数据安全」中与本阶段直接相关的条目；为不丢内容，整节全文收录，并标注主责阶段：

- accepted non_speech 空字符串可完整经过审核、Release、支持空目标的训练导出及 eval；pending/null 和 unintelligible 不得通过。→ **C / D**
- 一审填完不等于双审完成；同标注员自审、冲突未仲裁、旧 revision 导入均被拒绝。→ **C**
- 人工导入保留原 type/risk_tags/八路原文；人审 gold 与 candidate_text 独立。→ **C**
- 不同路径相同音频、同通话、同源切片、增强、已确认近重复跨集合均阻断发布。→ **A / D**
- eval 保留但未入选的同组样本也不得进入训练；缺 group 元数据不能静默退回按 ID 随机拆分。→ **A / D**
- 随机集名单不因 ASR、DNSMOS、分类或新模型结果变化；所有排除可追溯。→ **A / D**
- 输出配额不足、审计失败、标注未完成时不得宣称生成正式 Release。→ **D / C**
- 同输入、配置、版本、seed 在不同顺序/分片/续跑下得到相同分类、采样 ID 与内容摘要。→ **A / B / D**
- 重复发布幂等；同 ID 不同内容先失败，不覆盖既有文件。→ **D**

本阶段必须通过的验收重点：

- 八路身份登记、空/失败区分、守恒报告、按 ID+原音频哈希连接。
- 泄漏组连通分量、缺元数据进治理隔离池。
- 在查看模型错误前完成 eval_random / eval_core_reserve / calibration / dev 预留并输出不可变 reservation。
- 随机集名单不因后续 ASR、DNSMOS、分类或新模型结果变化。
- 缺 group 元数据不能静默退回按 ID 随机拆分。

## 7. 本阶段与母文档其余章节的衔接

- DNSMOS、四家族状态、风险与分类 → [012-B](./012-B-质量与分拣.md)
- 人工标注、双审、伪标签抽检门禁 → [012-C](./012-C-人工审核与伪标签门禁.md)
- 评测集/训练集配额、字段产物、原子 Release → [012-D](./012-D-数据集构建与Release.md)
- 业务指标与生产切换 → [012-E](./012-E-业务评测与生产切换.md)
