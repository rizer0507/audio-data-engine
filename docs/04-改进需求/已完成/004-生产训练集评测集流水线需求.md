> 复制本文件 → 另存为 `docs/04-改进需求/进行中/<编号>-简短中文名.md`，用自然语言把想法写清楚，交给 AI 即可。  
> **不用填表格、不用写 YAML、不用懂算子名。** 技术细节由 AI 按 `docs/07-操作手册/流水线构建-AI执行手册.md` 补全。

---

## 我想做什么

我需要在当前qwen_asr_batch.yaml和sensevoice_asr_batch.yaml的基础上重新构建一条流水线，能够实现一条相对通用的模型识别结果汇总流水线，用于将来我能够自由挑选识别结果得到金标、训练集、评测集



## 数据从哪来、结果要什么

- 输入大概是什么？（例如：已经清洗好的音频 / 某个文件夹 / 上一条流水线的结果）
qwen，sensevoice以及有可能的其他asr模型（kimi asr）得到的识别结果
- 跑完我希望得到什么？（例如：带 Qwen 识别结果的表、筛掉坏音频后的清单……）
一个带有所有模型结果的xlsx表格以及对应的parquet和jsonl
- 大概多少条数据？（有数就写，没有就写「不清楚」）
几十万条到上百万条



## 业务上有哪些规矩

（筛选条件、热词、阈值、要不要丢掉失败样本、和别的模型怎么比……想到什么写什么。）
筛选条件我后面会在你的流水线上进行后续的组装


## 我怎么才算满意

（例如：一条命令能跑；结果里要有某某字段；坏音频不能进最终结果……）
1、一条命令能跑，并且所有日志都能清晰看到，我能够通过日志得知当前工序②执行到哪一步，数据处理到哪一步，分片、并行处理是否有问题，模型并行识别是否有问题
2、暂时不同模型的识别请串行执行，不要并发执行模型推理，但后续可能考虑并发处理，这一点需要你妥善设计



## 其他我想说的

（机器、模型路径、参考哪条旧流水线、千万别动什么、截止日期……没有就空着。）
参考qwen_asr_batch.yaml+sensevoice_asr_batch.yaml


---

## 交给 AI 时可以说

```text
请严格按 docs/07-操作手册/流水线构建-AI执行手册.md 执行。
需求文档：docs/04-改进需求/进行中/xxx.md
用自然语言需求即可；缺关键信息先问我，其余工程细节你定。
做完更新 单条流水线执行命令.txt，保证一条 pipeline run 能跑。
```
