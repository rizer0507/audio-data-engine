# 005 · 评测流水线改进需求（外源金标 / 降依赖工序一）

> 状态：**已完成**（2026-09-04）。场景 1 / 场景 2 均已具备正式主路径。  
> 工序一外源收编详见 [006](./006-工序一清洗引擎拆分需求.md)；解耦底座见 [009](./009-评测流水线解耦落地.md)。

---

## 我想做什么

评测与训练解耦；金标可来自工序一产出或**外源 xlsx**，最终都进入 `eval register` → 独立跑批 → `eval_aggregate` → `eval_metric_pipeline`。

| 场景 | 金标来源 | 路径 |
| --- | --- | --- |
| **1** | 工序一 `summary_` / `gold_` / `classified_` | 直接工序三 |
| **2** | 外源 xlsx | `classify_external_gold` → `classified_*` → 工序三 |

---

## 判定结论

| 场景 | 状态 |
| --- | --- |
| **1** | **满足**（[009](./009-评测流水线解耦落地.md)） |
| **2** | **满足**（[006](./006-工序一清洗引擎拆分需求.md)） |

---

## 场景 1 命令簇

```bash
export BATCH=mt3000
export EVAL_NAME="eval_${BATCH}"
export EVAL_SOURCE="datasets/manifests/summary_${BATCH}.parquet"

audio-data eval register "$EVAL_SOURCE" --name "$EVAL_NAME"
audio-data eval check "$EVAL_NAME"

export QWEN_ASR_API_KEY=dummy
export QWEN_ASR_API_BASE=http://127.0.0.1:5560
audio-data pipeline run pipelines/qwen_asr_batch.yaml \
  --eval-name "$EVAL_NAME" --asr-run qwen

export QWEN_ASR_API_BASE=http://127.0.0.1:5561
audio-data pipeline run pipelines/qwen_asr_batch.yaml \
  --eval-name "$EVAL_NAME" --asr-run qwen-sft-epoch10

audio-data pipeline run pipelines/eval_aggregate.yaml \
  --eval-name "$EVAL_NAME" \
  --join-manifest qwen --join-manifest qwen-sft-epoch10

audio-data pipeline run pipelines/eval_metric_pipeline.yaml \
  --eval-name "$EVAL_NAME" \
  --eval-model qwen --eval-model qwen-sft-epoch10
```

---

## 场景 2 命令簇

```bash
audio-data pipeline run pipelines/classify_external_gold.yaml \
  --source-name "$BATCH" \
  --aggregate-base qwen3-asr \
  --external-gold path/to/外源金标.xlsx \
  --label-col label_text_raw \
  --force

audio-data eval register "datasets/manifests/classified_${BATCH}.parquet" \
  --name "eval_${BATCH}"
audio-data eval check "eval_${BATCH}"
# 其后同场景 1
```

过渡旁路（启动会 WARN，勿用于新任务）：`scripts/run_eval_from_label_xlsx.py`。  
更薄只算 CER：`scripts/eval_external_baseline_cer.py`。

---

## 可选后续（非本需求阻塞）

- 外源四桶向完整 `zh_asr_v1` 八桶语义靠拢（若业务需要）
- 择机删除旁路脚本

## 相关文档

- [评测流水线](../../03-流水线/评测流水线.md)
- [工序总览](../../01-项目架构/工序总览.md)
- `手册/*/03-工序三-评测.txt`
- [006](./006-工序一清洗引擎拆分需求.md) / [009](./009-评测流水线解耦落地.md)
